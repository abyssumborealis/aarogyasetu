# Dev: leave VITE_API_URL empty and the Vite proxy forwards API calls to VITE_PROXY_TARGET.
VITE_PROXY_TARGET=http://localhost:8000

# Production build: full URL of the deployed API (no trailing slash).
# VITE_API_URL=https://api.your-hospital.example
VITE_API_URL=
